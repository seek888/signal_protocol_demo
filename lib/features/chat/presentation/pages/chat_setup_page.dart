import 'package:flutter/material.dart';

class ChatSetupPage extends StatelessWidget {
  const ChatSetupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Chat Setup')),
      body: const Center(child: Text('Chat Setup Page - Coming Soon')),
    );
  }
}
