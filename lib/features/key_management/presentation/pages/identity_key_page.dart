import 'package:flutter/material.dart';

class IdentityKeyPage extends StatelessWidget {
  const IdentityKeyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Identity Keys')),
      body: const Center(child: Text('Identity Key Management - Coming Soon')),
    );
  }
}
